/* Tranformar función a arrow function de una linea

suma = function(a, b){
    return a + b
    } */

const suma = (a, b) => a + b;

/*verificación en consola*/
let total = suma(2,4)
console.log(total)